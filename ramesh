# Swapping two numbers using tuple unpacking
a = 5
b = 10

# Before swapping
print("Before swapping: a =", a, ", b =", b)

# Swap the numbers
a, b = b, a

# After swapping
print("After swapping: a =", a, ", b =", b)

